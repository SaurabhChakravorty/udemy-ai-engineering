# Munder Difflin final project dependencies
# Install from this directory:
#   pip install -r requirements.txt
#
# Or install the full module set from the parent folder:
#   pip install -r ../requirements.txt

-r ../requirements.txt
